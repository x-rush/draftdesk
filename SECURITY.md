# 安全边界

此版本为单人本地工作台。Compose 仅绑定 127.0.0.1；没有公网账户认证或多租户隔离。不要直接通过反向代理、端口映射或隧道公开管理界面。

API Key 保存在本机 data 目录中的 SQLite 配置里，并非加密保险库。保护该目录与备份，不上传到 GitHub。提交令牌只存散列且仅允许收件，可在界面撤销。收件内容默认私有、待审。

模型调用会把选定的研究材料或讨论上下文发送给配置的模型提供商。请先确认材料允许发送。评测脚本使用虚构样例，但真实模型调用仍可能计费。

若发现漏洞，请使用仓库启用的 Private vulnerability reporting（若可用）。不要把可用密钥、私人数据或完整攻击凭据放到公开 issue；若没有私密报告入口，可先提交不含细节的联系请求。本文不承诺尚未启用的报告渠道或响应时限。
