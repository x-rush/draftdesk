# 技术设计

Next.js 16 App Router / React 19 / TypeScript；Route Handlers 作为同源 API。Node 24 内置 SQLite，WAL + busy_timeout + 事务，集中保存证据、产物、计划、任务、讨论及密钥；单机持久目录由 Docker 挂载。无需 Redis。未来多实例/多租户应迁移 PostgreSQL 与认证，不将此本机版本直接开放公网。

Web 与 Worker 使用同一服务层和 schema。Worker 通过事务领取任务，每次一个，独立进程，不在 HTTP 回调里偷偷跑后台任务。租约过期标失败而不重复付费。每日计划以 planId + 北京日期唯一；失败可手动创建新任务。预算同时限制搜索次数、证据数、输出数、模型调用数、保守预留 tokens 和每日 tokens，实际用量单列，不冒充人民币报价。

研究阶段：受限确定性采集 → 证据聚合（AI）→ 对应领域 Skill 合成（AI）→ 审稿 Skill（AI）→ 引用与结构硬校验 → 私有入库。结构不合格最多一次修复，总次数/预算仍受约束。引文必须原文匹配；摘要只是摘要。模型不得执行证据里的指令。

外部收件使用专用令牌散列校验，提交 ID 加连接身份幂等，冲突 409，内容指纹复用；完整事务确保重试不产生半份结果。外部草稿永远待审核，不继承对方声称的可信状态。提交不隐式触发付费分析，用户选择分析后走同一 Worker。

来源请求 DNS 解析后固定公网 IPv4 连接，不跟随重定向，限制协议、端口、正文大小和超时。模型端点限制百炼官方域；来源、模型和脚本无任意 shell 执行。密钥不导出、不回显；本机接口检查 Host 与 Origin，外部工具令牌只允许 intake。

质量评测区分确定性测试、模拟端到端和真实模型评测。包含薄证据、虚构引用、趋势无数字、应用无付费证据、宏大新闻无个人影响等反例。Skills 由项目原创实现成熟研究方法，并记录参考来源，不直接复制或执行第三方未知脚本。
